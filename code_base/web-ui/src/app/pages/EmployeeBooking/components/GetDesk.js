import { Typography } from '@material-ui/core';
import React from 'react';

export const GetDesk = () => {
    return (
        <div style={{ padding: '0 0 24px 5px'  }}>
            <Typography style={{ fontSize: 16 }}>A desk will be selected automatically</Typography>
        </div>
    )
}