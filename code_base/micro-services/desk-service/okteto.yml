name: desk-service
image: okteto/maven:latest
command: bash
workdir: /okteto
volumes:
  - /root/.m2
sync:
  - .:/okteto
forward:
  - 8002:8002
resources:
  limits:
    cpu: "1"
    memory: 2Gi
  requests:
    cpu: "0.5"
    memory: 2Gi